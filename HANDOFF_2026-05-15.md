# Handoff Note — 2026-05-15

다음 세션 진입 시 이 파일 + 메모리 `project_odin2_datapopup.md` 부터 확인.

---

## 1. Repo 기준 상태

- HEAD = `2992225` (test: add BUG-25175 THOR2 RY07260302M regression evidence (17/18 PASS))
- origin/master 대비 **ahead 1** (2992225 미push — 사용자가 "푸시는 나중에" 결정)
- BUG-25175 작업 마감 (17/18 PASS). 어제 세션 분 commit 완료, push 보류.

## 2. 오늘(2026-05-15) 주 작업 — ODIN2 Z0513U DataPopup race

### 2-1. 이슈
USB 연결 상태 reboot → 모바일 데이터 "사용" popup 응답 시 UI는 데이터 ON이나 실제 인터넷 불가 (intermittent 2~3/10). 단말 ODIN2(AT-M150) `<device_serial>`, 빌드 Z0513U, SKT USIM.

### 2-2. 산출물 (미커밋 working tree)
| 파일 | 상태 |
|------|------|
| `scripts/data_popup_repro_loop.py` | 반복 테스트 도구 (검증 완료, 정상 작동) |
| `BUG_DataPopup_Monitor.bat` | Windows 런처 |
| `logs/data_popup_repro/<run_id>/` | 5개 run 산출물 (results.csv + per-cycle logcat). 정책상 commit 보류 |
| `_datapopup_now.{xml,png}` / `_datapopup_dialog.xml` | 루트 scratch (dialog 구조 분석용, 정리 대상) |

### 2-3. 결과
- 도구 3-bug 수정 + USB re-enum hardening 후 **검증 완료**.
- 본 데이터 수집: **9/9 PASS** (V1, 6 full-sampling + 3 fast). 본 버그 0건 재현.
- 9/9 PASS 확률 ≈4~7.5% → 자동 흐름이 manual race 회피 판단. 가설 H-A(adb-stable 대기가 race window 스킵) 유력.
- USB composition reboot마다 drift (UI=V1 고정인데 런타임 V1↔V2 교대) = 별 라운드 BUG-GAP 후보.

## 3. 다음 세션 진입점 (사용자 명시)

**V2 composition 먼저 진행.**
1. 사용자가 히든메뉴에서 V2(MODEM+DM+RMNET+ADPL+QDSS+ADB) 설정 + 시인
2. QXDM 세션 시작 확인
3. `python scripts/data_popup_repro_loop.py -n 10 --variant V2 --serial <device_serial>`
4. 미재현 시 → H-A 검증용 early-tap 변형(adb-stable 대기 생략) 옵션 설계

## 4. 별 라운드 후보
- BUG-GAP: USB composition UI(V1 고정) vs 런타임(V1↔V2 drift) 불일치 — `project_at_m150_usb_persist_bug` 동일 증상군, Z0513U 재현
- 도구 polish: 루트 `_datapopup_*` scratch 정리, 단말×앱 폴더(`ODIN2 - BUG_DataPopup/Z0513U/`) 구조로 evidence 이관
- 9/9 PASS 자동 미재현 결과 자체를 REGRESSION_RESULT 형식 문서로 누적

## 5. 정책 / 미결
- `2992225` push 보류 중 (사용자 "나중에"). 다음 push 시 ahead·파일 audit 필수, fast-forward만.
- ODIN2 DataPopup 산출물 commit 여부 = 사용자 결정 영역 (BUG-25175 evidence commit 선례 있음, 동일 패턴 적용 가능하나 명시 승인 필요).
- 도구/logs 모두 미커밋 working tree. 글로벌 commit policy = 명시 승인 시에만 commit.
