# HANDOFF 2026-05-20

세션 종료 저장. 커밋은 글로벌 정책대로 보류(아래 "커밋" 참조).

## 단말/환경

- ODIN2 = AT-M150, adb serial **`<odin2_device_serial>`**.
- 세션 종료 시점 단말 USB composition = **V2** (`diag,serial_cdev,rmnet,dpl,qdss,adb`).
- AT-M140 (`<thor2_device_serial>`) 동시 연결 상태 — `--serial` 명시 필수.
- 빌드 = Z0518U, SIM = LG U+.

## 트랙 2 — BTS18697 [ODIN2][WCDMA][DEBUG] IMS IP Address

세션 후반 신규 작업. PDF 정독 → 테스트 계획 → Z0520U_Daily 빌드 1차 검증.

### 결론
- **LTE DebugScreen**: `IMS IP:` 표시 + ground truth와 완전 일치 = **runtime PASS** (Gerrit I96de352c 정상 동작)
- **WCDMA DebugScreen**: `IMS IP:` 라벨 자체 누락 = **BUG-GAP observed** (수정이 WCDMA layout에는 미반영)
- IMS PDN은 WCDMA에서도 정상 활성 (rmnet_data2 IPv6, P-CSCF, MMTEL capability) — 시스템·모뎀엔 IMS IP 보유, DebugScreen WCDMA layout만 표시 경로 없음

### 산출물
- `ODIN2 - DebugScreen BTS18697/RESULT_2026-05-20.md` — 정형 결과 보고서 (개발자 전달용)
- `ODIN2 - DebugScreen BTS18697/evidence/` (4 dirs: sanity, T1, T1-LTE, T1-WCDMA-final) — UI dump, screen, ifconfig, dumpsys IMS, ground truth
- **개발자 요청 핵심**: WCDMA DebugScreen layout에 LTE와 동일한 `IMS IP:`/`IMS version:`/`pcscf:` 필드 추가 부탁

### 미수행 (수정본 후 진행)
T2~T6 (Airplane/SIM swap/RAT change/Manual 설정 갱신 검증) — WCDMA에 표시 자체가 없어 갱신 테스트 무의미. WCDMA 측 수정 후 재개.

---

## 주요 발견 (BUG-25796)

**Root cause = Windows 호스트의 `WWAN AutoConfig` 서비스가 USB 연결 단말 모뎀에 간섭** → PDN bringup race(0xffff loop) 유발. **34 사이클 6조건 교차 + 정방향 인과 재현 + carrier 일반화로 확정**.

| 조건 | N | PASS | PASS-delayed | FAIL-recover | post-tap `0xffff` 빈도 |
|---|---|---|---|---|---|
| V2 WWAN-**on** (LGU+) | 1 | 0 | 0 | **1** (130.6s) | 1/1 |
| V1 WWAN-**on** (LGU+) | 10 | 0 | 8 | **2** (둘 다 130.6s) | **10/10 (100%)** |
| V1 WWAN-**off** (LGU+) | 10 | 0 | 10 | 0 | **0/10 (0%)** |
| V2 WWAN-**off** (LGU+) | 3 | 1 | 2 | 0 | **0/3 (0%)** |
| V1 WWAN-reon (LGU+) | 5 | 0 | 3 | **2** (130.9, 130.6) | **4/5 (80%)** |
| **V2 WWAN-on (SKT)** | **5** | **4** | **1** | 0 | **5/5 (100%)** |
| 누적 WWAN-on (전체) | **21** | 4 | 12 | **5 (24%)** | **20/21 (95%)** |
| 누적 WWAN-off | 13 | 1 | 12 | 0 | **0/13 (0%)** |

- **WWAN-off에서 `0xffff` trigger 자체가 사라짐** (단순히 FAIL 분류가 없는 게 아니라, 잠깐 bringup 실패 신호조차 안 나옴)
- **FAIL-recover의 `recover_sec` 5건 모두 ≈130.6s** (130.6×4 + 130.9, 평균 130.66s σ=0.13s) = 모뎀 측 deterministic backoff/retry 타이머 quantum
- **WWAN 토글로 trigger on/off 재현** (정방향 인과 입증)
- **SKT 추가 검증**: trigger는 carrier-agnostic (LGU+·SKT 동일 95~100% 발현), FAIL 진입은 carrier-dependent (LGU+ 31% vs SKT 0%, n=5)
- Composition(V1/V2)은 trigger 발현에 결정적 영향 없음, host WWAN 상태가 결정 변수
- WWAN-on은 필요조건이나 충분조건은 아님 (실측 발현률 ~95%)

상세 분석 = `doc/BUG25796_Mobile_Data_sync_issue/ANALYSIS_2026-05-20.md`.
BUG_REPORT_25796.md 상태/빈도 항목 갱신 완료.

## 본 세션 진행 (2026-05-20 09:50 ~ 13:51)

1. **이전 vs 수정 스크립트 차이 설명 완료** — 옛 게이트(ping4)·옛 창(60s)·옛 분류(t0-only MOBILE_OFF) 세 결함이 BUG-25796 반대 가정 → 신호 차단. 수정 후 IS_VALIDATED + 180s recovery-watch + 전체창 md 게이트로 검출.
2. **adb log 보강 (사용자 승인 후 코드 수정)**: `scripts/data_popup_repro_loop.py` L252-264, `-G 16M × 5 buffer (main/radio/system/events/crash) + -b all` (dump timeout 60s). 1회 실런 검증 OK.
3. **recovery-watch 1회 실런 PASS** (V2 WWAN-on): run `20260520T095048`, `FAIL-recover recover_sec=130.6s` (1분 screen timeout 조건).
4. **screen timeout 1분 → 30분 변경** (사용자, 자동 OFF 영향 제거).
5. **V1 × 1 (WWAN-on)** = run `20260520T110501`, `PASS-delayed`. signature `0xffff x1`.
6. **V1 × 10 (WWAN-on)** = run `20260520T110915`, tally `{PASS-delayed:8, FAIL-recover:2}`. 두 FAIL 모두 `recover_sec=130.6s`. 매 사이클 `0xffff x1`.
7. **개발자 가설 수령**: WWAN AutoConfig 의심. 사용자가 PC측 서비스 사용 중지.
8. **V1 × 10 (WWAN-off)** = run `20260520T115146`, tally `{PASS-delayed:10}`, 매 사이클 `0xffff x0`. 가설 강력 지지.
9. **V2 × 3 (WWAN-off)** = run `20260520T134410`, tally `{PASS:1, PASS-delayed:2}`, 매 사이클 `0xffff x0`. composition 일반화.
10. **V1 × 5 (WWAN-reon, 정방향 교차)** = run `20260520T140623`, tally `{PASS-delayed:3, FAIL-recover:2}` (FAIL 40%), 매 사이클 sig 4/5. recover_sec 130.9·130.6s. 인과관계 정방향 입증.
11. **V2 × 5 (WWAN-on, SKT, carrier 일반화)** = run `20260520T150647`, tally `{PASS:4, PASS-delayed:1}`, 매 사이클 sig 5/5. FAIL 0. SKT에서도 trigger 100% 재현 = host-side cause carrier-agnostic 입증. SKT는 130초 backoff 진입은 안 함 (carrier별 PDN 정책 차이 추정).
12. **doc 갱신**: `ANALYSIS_2026-05-20.md` (34 사이클 6조건 표 + carrier 일반화), `BUG_REPORT_25796.md` 전면 재작성 + SKT 정정 이력 추가, `SUMMARY_REPORT.md` SKT 반영 (현재 상태 = WWAN root cause + carrier 일반화 + 회피책).

## 남은 검증 (다음 세션 후보, 우선순위순)

1. ~~**WWAN 재활성 ×5**~~ → **완료 (V1-WWAN-reon n=5, 정방향 재현 입증)**.
2. **V2 × 10 (WWAN-off)** — 오늘 V2-WWAN-off n=3은 작은 표본. n=10 누적하면 V2 일반화 더 단단. ~15분.
3. **130.6s timer 식별** — QXDM `0x713A`/`0x1544` 로그 분석으로 표준 timer 정체(3GPP T3396 vs QMI 내부 retry vs ds_rmnet_meta_sm.c 상수) 확정.
4. **다른 호스트 OS 교차** — macOS/Linux에서 같은 USB reboot+consent 시 발현 여부 (가설 일반화).
5. **MBIM/QMI/diag 중 어떤 경로가 간섭원인지** — Wireshark USBPcap 또는 모뎀 diag trace 분석.
6. **(BTS18697)** WCDMA layout 수정본 수령 후 T2~T6 진행 (Airplane/SIM/RAT change/Manual 갱신 검증).
7. **(BUG-18453)** T3 스펙 의도 확인 (보고자/개발).

## 커밋

전부 untracked/미커밋(글로벌 정책 = 작업 중 commit 금지, 하루 종료 batch는 예외이나 **사용자 명시 승인 필요**). 본 세션 신규/수정 후보:
- `scripts/data_popup_repro_loop.py` (16M×5 buffer + -b all 수정, untracked 유지)
- `logs/data_popup_repro/20260520T{095048,110501,110915,115146,134410,140623,150647}/` (7 runs · 6 조건 · 34 사이클)
- `doc/BUG25796_Mobile_Data_sync_issue/evidence/` (버그리포트 인용 evidence 일체 복사본)
- `doc/BUG25796_Mobile_Data_sync_issue/SUMMARY_REPORT.md` (사내 톤다운 요약본)
- `ODIN2 - DebugScreen BTS18697/` (RESULT_2026-05-20.md + evidence/{sanity,T1,T1-LTE,T1-WCDMA-final})
- `doc/BUG25796_Mobile_Data_sync_issue/ANALYSIS_2026-05-20.md` (신규)
- `doc/BUG25796_Mobile_Data_sync_issue/BUG_REPORT_25796.md` (상단 UPDATE 배너 + 상태/빈도 갱신)
- `HANDOFF_2026-05-20.md`
- 어제부터 누적된 batch 후보(HANDOFF_2026-05-19.md 참조): `ODIN2 - DebugScreen BUG18453/`, `doc/BUG25796_Mobile_Data_sync_issue/` 어제 파일들, `HANDOFF_2026-05-19.md`, `scripts/lgu_consent_diag.py`

사용자가 "지금 commit/배치 커밋해" 명시 시에만 실행.

## 진입 순서 (다음 세션)

1. `HANDOFF_2026-05-20.md` (본 파일) → `doc/BUG25796_Mobile_Data_sync_issue/ANALYSIS_2026-05-20.md` 순으로 읽기
2. `adb devices` 로 ODIN2 serial 재확인 (`<odin2_device_serial>` 유지 여부, AT-M140 동시연결 가능성)
3. USB composition 현 상태 확인 (`getprop sys.usb.config`)
4. WWAN AutoConfig 현 상태 확인 (사용자에게 질의)
5. 다음 검증 항목 우선순위 사용자와 정렬 후 진행
6. commit/push는 항상 명시 승인, force 금지
