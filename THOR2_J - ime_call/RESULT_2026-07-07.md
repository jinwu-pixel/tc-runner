# RESULT 2026-07-07 — THOR2_J ime_call 다이얼러 하드키 이슈

## 세션 결과 블록
- **실행일**: 2026-07-07
- **단말**: ALT AT-M140 (alt_thor2 / thor2, Android 14 UP1A.231005.007 `SELJY072606MZ0706`) — 3 유닛 교차
  - `B2700125BW000083` (원 신고 단말)
  - `B24201259J000043` (REF, 최소쌍 검증)
  - `B2700125BW000115` (fresh, 주입키·모드 검증)
- **앱**: `com.android.dialer` 1.0.0.1638 (벤더 `ls_dialer` 빌드). IME = iWnn `jp.co.omronsoft.iwnnime.ml` 2.23.1.10
- **범위**: 하드 키패드 입력의 (1) `*` 삼킴 (2) 숫자 이중
- **PASS**: manual evidence observed (실기 3-way: kernel `getevent` / 필드 육안 / a11y 이벤트 스트림)
- **신규 발견**: `*` 삼킴 = a11y 트리거 결함 **CONFIRMED**
- **다음 확인**: 숫자 이중 야생 포획 하니스, 개발 제보

---

## ISSUE-1 — 하드키 `*` 삼킴 (진단: CONFIRMED / 이슈: OPEN)

### 증상
iWnn 기본 상태에서 다이얼러 번호 필드에 하드 `*` 입력이 소비되어 미입력. 숫자·`#`는 정상.

### 근본 원인 (규명)
**트리거 = 접근성/자동화(a11y) 연결.** UiAutomation(`uiautomator dump`/`events`, Appium 등) 또는 TalkBack이 한 번 연결되면, 이후 다이얼러 재실행 시 iWnn이 하드 `*`를 기능키로 소비. **재부팅 전까지 sticky.** locus = iWnn ↔ ls_dialer digits(phone) 필드 하드키 경로.

### 결정적 최소쌍 (REF `B24201259J000043`, 재부팅 클린 baseline)
| 시행 | 조건 | `*` |
|---|---|---|
| R4 | 재부팅 후 · 계측 0 · 일반 경로 | **통과** (`3*#`) |
| R5 | + `uiautomator dump` **1회**(트리거) → 재실행 | **삼킴** (`3#`) |
| R6 | 재실행 반복 | **삼킴** (sticky) |

유일 변수 = a11y 연결 1회 → 인과 확정.

### 보강 재현 (정/역)
- **IME A/B** (083·115): iWnn OFF → 전 키 정상 2/2 (`36*#`) / iWnn ON → 삼킴. 원인 = iWnn
- **물리키 전용** (115): `adb shell input keyevent`(가상 deviceId=-1) 주입 `*`는 **삼키지 않음**(`6*#`) → 결함은 물리 키패드(deviceId=3, mtk-kpd)↔IME 계층 한정
- **단말 무관**: 083·043 교차 재현. kernel `getevent`는 전 프레스 `KEY_NUMERIC_STAR` 1 DOWN/UP clean(하위 계층 정상)

### 정량
- 정착 상태 삼킴: 5/5 세션 (083 T-series 3 + 043 R5/R6 2), 프레스 6/6
- a11y-free 통과: R4 + 083 iWnn-OFF + 115 E1/F2 = 4/4
- 물리 프레스 kernel 검증 100% (getevent)

### 재현 절차 (개발 제보용)
1. iWnn 기본, 접근성/자동화 도구(TalkBack·UiAutomator·Appium 등) 실행 이력 있는 상태
2. 다이얼러 실행 → 하드 `*` → 미입력
3. (역) 재부팅 or iWnn 비활성 → 정상 복귀

### 실사용 영향
하드키로 `*`/`#` 발신 코드(`*141#` 류) 입력 불가. 노출군 = **TalkBack 등 접근성 서비스 사용자**(본 기종 = 시니어 대상 simplemode 탑재) + UiAutomator/Appium 계열 QA·자동화 환경.

### 제보 방향
- **ls_dialer (ALT/hnlens)**: phone-class digits 필드에서 IME 하드키 개입 차단(pass-through 보장)
- **iWnn (OMRON)**: `TYPE_CLASS_PHONE` 필드 하드키를 기능키로 소비 금지

---

## ISSUE-2 — 하드키 숫자 이중 입력 (진단: OBSERVED / 이슈: OPEN)

### 증상 (사용자 최초 관찰, 2026-07-07 11:18 세션)
하드 숫자키 1프레스 → 다이얼란에 숫자 2회 입력. `*`/`#`는 1회(정상). 다이얼패드 온스크린 **첫 터치**로 치유, 이후 재현 안 됨.

### 오늘 재현 시도 (전부 실패, 육안·클린 측정)
| 조건 | 결과 |
|---|---|
| 클린 / 数字 모드 잔존 / **かな 모드 잔존**(E1) | 정상 (`36`) |
| + a11y (dump·live UiAutomation·TalkBack) (D5·D6·F2) | 정상 or `*`삼킴 (이중 아님) |

### 미재현 분석
1. **phone 필드 강제 numeric passthrough** — かな 모드라도 다이얼러 필드 attach 시 iWnn이 숫자 직접입력 전환(E1 실증). 텍스트 commit(이중 전제) 미발생
2. **one-shot transient** — 사용자 최초 1회 후 본인도 재진입 실패. 첫 터치 치유 후 소실
3. **관측자 효과** — 필드 adb 판독(=a11y 연결)이 상태 교란. 육안만 무손상

### 상태
버그리포트(`bugreport-*.txt`) 정황: 입력 파이프라인 전 계층(reader/HLS/dispatcher)은 1회 대칭 전달 입증 → 이중은 미로깅 텍스트 계층(IME/app). 강제 재현 불가한 희귀 상태로 **OBSERVED 유지**. 야생 포획 하니스로 장기 추적(→ `capture_harness.sh`).

---

## 시행 이력 (요약)
- 083: T1~T9 — `*` 삼킴 규명, IME A/B, "2회차" 가설 → a11y 트리거로 정정
- 043: R1~R6 — 재부팅 클린 최소쌍(R4 통과/R5 삼킴/R6 sticky) = a11y 인과 확정. D2~D6 모드·TalkBack 매트릭스
- 115: 주입키 검증(물리 전용 확정) · E1(かな no-a11y) · F1/F2(かな+a11y) — 이중 미재현

## 검증 프로토콜 (재사용)
- 3-way: `getevent -lt`(물리 프레스, a11y-free) / 필드 육안(무손상 판정) / `uiautomator events`(a11y 스트림 — 단, 연결 자체가 트리거)
- **화면 터치 금지**(터치=치유 교란). 매 시행 fresh 재실행(종료키→홈→숫자 launch)
- `input keyevent` 주입 = 가상 device → 물리 재현 불가, 사용 금지
- `ime disable/enable/set` = iWnn 프로세스 미종료·모드만 섭동

## 증거 자산
- 버그리포트: `bugreport-alt_thor2-*.txt` (+ 12-agent 워크플로 분석 wf_4d6cdf79-0d5)
- kernel getevent 로그 (세션 scratchpad: bilpih5sq/bw1qkj8wm/ba9rkbe65)
- a11y 이벤트 스트림 (b384wahqo — かな 프라이밍 캡처)
