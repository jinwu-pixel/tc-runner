# HANDOFF — tc-runner 세션 인수 (2026-07-21)

## 0. 현재 상태 (그대로 신뢰 가능)
- repo: `c:\Users\momen\Projects\tc-runner`
- **HEAD `742445a` = origin/master · ahead/behind 0/0 · staged 0 · tracked dirty 0** — push 완료된 깨끗한 재개 지점
- 직전 세션: gap-8 Stage 1/T1 실기 → 문서화 → commit → push 완주
- 단말 F0(`B06201249E0002F0` / `AT-M140`): 세션 종료 시 연결됨. **USB 이탈↔복귀 반복** 주의

## 1. ⚠ 단말 baseline이 변경됨 (영속·중요)
직전 세션 `pm clear #1` 결과로 F0 launcher가 **4-page → 3-page**, **사진 위젯 page 제거**됨.

- 현재: MediaStore **0** / PFWSEED 0 / pkg **218** / HOME role `com.hnlens.simplemode` / **3-page**(p0 단축다이얼·p1 시계앱·p2 도구는 baseline과 구조 동일)
- **`사진 추가하기` 진입점 부재** → PFW 관련 작업 현 상태 실행 불가
- 복원 동선 **미매핑**. 임의 복원 시도 금지

## 2. 운영 규칙 (필수)
- **글로벌 commit 정책**: 작업 중 commit/push 금지. 명시 "commit now"/"push now" 또는 5예외에만. **exact-path stage만**, broad add 영구 금지, force 금지, fast-forward만
- **커밋 전**: `git status --short` + `git diff --name-only --cached`. 예상 외 파일 보이면 즉시 중단·보고
- **pytest는 `tests/` 스코프**: `venv/Scripts/python.exe -m pytest tests/ -q -p no:cacheprovider` (repo root 전체는 선존 collection 오류). baseline **1120 passed** (직전 세션 기록, 이번엔 미재실행)
- **보고 어휘**: 단독 `PASS` 금지 — `validate PASS` / `runtime PASS` / `manual evidence observed` / `BUG-GAP observed`. precondition 확인은 `runtime precondition matched`
- **승인 게이트**: 코드·TC 편집, 외부 시스템 변경 호출, commit/push 전부 명시 승인

## 3. 단말 작업 함정 (직전 세션 실측)
- **remote dump는 `/data/local/tmp`만** (`/sdcard` 금지 — MediaStore 오염·cleanup 부담). 잔여 시 F0 sole+model 재확인 후 **해당 1파일만** 삭제
- **adb는 PowerShell로 호출** — Git Bash가 `/sdcard`·`/data/local/tmp`를 Windows 경로로 mangle
- **PowerShell에서 `'\tdevice$'` 류 정규식 금지** — 하네스 가드가 Remove-Item 경로로 오탐해 스크립트 전체 차단. device 파싱은 tab-split(`$ln.Split([char]9)`, `$p[1] -eq 'device'`)
- **tap/dump는 한 호출에 원자적으로**(wake→nav→focus 확인→tap→dump). 호출 사이 화면 sleep 시 focus=null·subdisplay top → dump null root, tap이 no-op
- F0는 dual-display 폴더폰 — `topResumedActivity`에 `com.hnlens.app.subdisplay` 혼입

## 4. 열린 게이트 (전부 승인 게이트)

### A. Slice 0.5 — canonical contract drift ledger (host-only·무단말·즉시 가능) ★권장
- 설계 `docs/superpowers/specs/2026-07-14-canonical-execution-contract-design.md` §6 + Task 1
- 선행조건 충족(Stage 0 push · tests/ 1120 green · SSOT=CLAUDE.md canonical)
- 산출: `scripts/contract_drift_ledger.py` + `tests/test_contract_drift_ledger.py` + `reports/contract_drift/<digest>/{contract_drift_matrix.csv, SUMMARY.md}`
- 수용(§6.6): byte-deterministic 2회 동일 · 8 producer×consumer pair · corpus 3/25/0/2 · 확정 결함 2 재현. 필수 RED fixture `test_input_text_text_is_not_selector_alias`
- ledger 산출 후 **STOP → 리뷰** 후에만 Slice 1a

### B. gap-8 Stage 2 — **BLOCKED** (재개 3조건 전부 충족 필요)
- 문서 `THOR2 - ALT Basic TC Audit/PHASE2_DIRECTIVE_DRAFT_C11_GAP8_2026-07-20.md` §0
- ① 사진 위젯 **page 추가 동선** dump-first 매핑 ② 해당 mutation **별도 승인** ③ 종료 기준 재정의(`3-page 수용` / `4-page empty 복원` / `T2 유지` 중 택1)
- **매핑 완료(재사용)**: 위젯 *구성* 동선 = `사진 추가하기` → `포토 슬라이드 쇼` 팝업(`다시 보지 않기` 미접촉·`확인`) → `com.hnlens.simplemode/.ui.photopick.PhotoPickActivity` → 사진 tap → `저장`(`tv_done`). picker counter `tv_proportion` = **N/10**(최대 10)
- **미매핑**: page 추가(생성) 동선

### C. 기타 단말 대기 (비긴급)
- eng-mode 런너 **device smoke**(ODIN2): preflight + 대표 caseset 1회 — 통과 전 runtime 동등성 주장 금지
- Engineer IMS **KT/LGU+ 재캡처**(ODIN2+USIM, RECAPTURE_RUNSHEET 준비됨)
- ALT **Part B 236**(F0, thor2j-side driver 경유) — 실질 throughput 레버

## 5. 미접촉 유지
- `…/catalog/f0_c11_nav_2026-07-01/discovery_seed_2026-07-13/s2_pmclear_g2_stage1_2026-07-20/`의 **raw XML 12 + PNG 3 = local-only, staging 금지**(manifest.md만 커밋됨)
- `scripts/usb_composition_verify.py` + `ODIN2 - USB Composition BTS25462/` = 별도 트랙, 미커밋 유지
- sibling repo `C:\Users\momen\Projects\thor2j-tc-appium` — cross-commit 금지
- backlog untracked ~1,689 — 지시 전 stage 금지

## 6. 직전 세션 핵심 발견 (재해석 주의)
`742445a` / `RESULT_MEDIA_SEED_C11_GAP8_2026-07-20.md` Part 2:

- **pm clear는 위젯을 "빈 앨범으로 되돌리지" 않고 page 자체를 제거**(4→3). 4-page canonical 복원 **실패**
- **taxonomy 4종**(구조만으론 stale 판별 불가): 21-node=구성 shell / stale=shell+미디어 소실→깨진 render / 20-node=page 존재·빈 상태 / 3-page=page-absent
- "4-page empty = launcher default 아니라 선존 사용자 구성" 가설 = **`OBSERVED`**(CONFIRMED 아님)
- `pm clear runtime PASS` **주장 금지**
- Part 1 media-count 상관 해석은 **Part 2 taxonomy가 supersede**

## 7. 다음 첫 행동 제안
**A(Slice 0.5)** — 무단말·선행조건 충족·즉시 착수 가능. B는 재개 조건 ① 매핑(read-only 탐색)이 관문이고 단말 창 필요.

시작 전 `git status --short`로 clean·0/0 재확인.
