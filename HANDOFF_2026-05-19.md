# HANDOFF 2026-05-19

세션 종료 저장. 커밋은 글로벌 정책대로 보류(아래 "커밋" 참조).

## 단말/환경

- ODIN2 = AT-M150. **adb serial 변동**: 세션 중 `<previous_device_serial>` → `<current_device_serial>` (USB 재열거 추정, product=alt_odin2·model=AT_M150 동일 타깃). 다음 세션 `adb devices`로 재확인 필수.
- 2단말 동시 연결(ODIN2 + AT-M140 thor2) → 항상 `adb -s <serial>`.
- ODIN2 빌드 = Z0518U(테스트빌드), SIM = LG U+. 세션 종료 시 mobile_data ON, 데이터 회복 상태(dc=2).

## 트랙 1 — BUG-18453 ODIN2 DebugScreen internet/tethering/DUN IP

- 산출물: `ODIN2 - DebugScreen BUG18453/` (RESULT_2026-05-19.md, TC_BUG18453_reverify.md, MANUAL_GUIDE.md, evidence/01~12)
- 최종: T1 PASS / T2 PASS(WIFI+USB) / **T3 BUG-GAP("DUN IP:" 문구 상시 노출, 지시#3 상충)** / T4 WIFI=지연 clear·USB=즉시 clear
- 잔여: **T3 최종 스펙 의도 확인(보고자 Sh hwang/개발 YanLijie)** = 정식빌드 재검증 PASS 기준 결정. T4 WIFI clear latency 미측정.

## 트랙 2 — BUG-25796 Mobile Data consent 후 UI ON·실인터넷 불가

- **재현 확정**(logcat 권위신호 + QXDM). 산출물: `doc/BUG25796_Mobile_Data_sync_issue/` (BUG_REPORT_25796.md, ANALYSIS_2026-05-19.md, qcat_*.txt, ls_log/, 11.png, .mp4)
- Root cause(모뎀, QCAT+QSR4): consent tap 후 PS-routing/ACL **IP-family 불일치**(`ps_aclrules IFACE_AND_POL_FAMILY_MATCH (No Match)` → `ps_route Routing failed net_down_reason:0x200f1 acl_deny:-3` → `failed to find Iface` errno202 → QMI CALL_FAILED/WDS_VCER_INTERNAL 241 → RIL 0xffff). NAS 미진입. 망 reject·consent-key race 아님.
- **성격 정정**: 영구 아님 → consent 후 ~수 분 실인터넷 불가 후 **지연 회복**(trial2: tap 19:31:16 → ~19:34:29 회복, latency≈1~3분). 리포트/분석/메모리 정정 완료.
- 도구 보강 `scripts/data_popup_repro_loop.py`(untracked, 컴파일 OK):
  - classify MOBILE_OFF 게이트 t0-only 결함 수정(전 샘플 md=0일 때만 MOBILE_OFF)
  - signature post-tap 경계 줄인덱스→logcat 타임스탬프 기반, 1차 신호 onDataNetworkSetupDataFailed(internet.lguplus,0xffff)
  - recovery-watch 추가: FAIL→`FAIL-recover`(latency)/`FAIL-persistent`, **`--recover-budget` 기본 180s**(사용자 결정)
  - CSV +`tap_after_boot_sec`/`fail_phase`/`sig_0xffff_posttap`, per-cycle `signature.txt`/`qxdm_join.txt`(KST+UTC)
  - 검증: trial2 = `FAIL-0xffff-loop` 정상 검출. recovery-watch 실런 검증은 미수행.

## 내일 할 일 (사용자 명시)

1. **"이건 내일 수정"** — 사용자가 내일 수정 예정. 대상 명확화 필요(스크립트/추가건 — 다음 세션 시작 시 확인).
2. **"이전 스크립트에선 왜 이 이슈가 안 나왔는지" 답변** — 오늘 답 보류, 내일 설명. (참고 단서: 옛 ping4 프로브 무효(IS_VALIDATED로 교체)·classify MOBILE_OFF t0-only 오판·짧은 샘플창으로 지연회복/실패 미포착·USB composition/serial 변동·tap 타이밍 등 — 내일 정리해 답변)
3. recovery-watch 실런 검증(`-n 1 --recover-budget 180`)로 `recover_sec` 실측.
4. (트랙1) T3 스펙 의도 확인 후 정식빌드 재검증.

## 커밋

전부 untracked/미커밋(글로벌 정책 = 작업 중 commit 금지, 하루 종료 batch는 예외이나 **사용자 명시 승인 필요**). batch 후보:
- `ODIN2 - DebugScreen BUG18453/` 일체
- `doc/BUG25796_Mobile_Data_sync_issue/` (BUG_REPORT/ANALYSIS/qcat_*.txt; ls_log·.qmdl·.mp4 대용량 — 포함 여부 사용자 결정)
- `scripts/data_popup_repro_loop.py` (+ lgu_consent_diag.py)
- `HANDOFF_2026-05-19.md`
- 메모리(`~/.claude/.../memory/`)는 git 비대상

사용자가 "지금 commit/배치 커밋해" 명시 시에만 실행.
